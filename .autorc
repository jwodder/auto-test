{
    "onlyPublishWithReleaseLabel": true,
    "baseBranch": "master",
    "author": "auto <auto@nil>",
    "noVersionPrefix": true,
    "plugins": [
        "git-tag",
        [
            "exec",
            {
                "afterAddToChangelog": "bump2version \"$(echo \"$ARG_0\" | jq -r .bump)\"",
                "afterRelease": "python -m build && twine upload dist/*"
            }
        ]
    ]
}
